import { template } from '../src/pg-html-compiler';

describe('Pregular node compile template', () => {
  it('should test', () => {
    debugger;
    expect(123).toEqual(123);
    console.log('Pregular node compile template!', template);
  });
});
