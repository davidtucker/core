import { updateDom } from '../src/pg-dom-utils';

describe('Pregular browser dom utils', () => {
  it('should test', () => {
    expect(123).toEqual(123);
    console.log('Pregular browser dom utils!', updateDom);
  });
});
